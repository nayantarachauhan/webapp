name: CI-CD pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    

    steps:
    - name: checkout
      uses: actions/checkout@v2

    - name: Docker Login
      uses: docker/login-action@v1
      with:
        username: nayantara52
        password: Vkssann@2805

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: nayantara52/nayan-docker-images:v1

    - name: AWS Login
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIARWAZI6BRA2BMG36N
        aws-secret-access-key: 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
        aws-region: us-east-2

    - name : change mode
      run : |
        chmod 600 ./ec2-key-pair.pem
      
    - name: pull image to ec2
      run: |
        ssh -o StrictHostKeyChecking=no -i ./ec2-key-pair.pem ec2-user@ec2-3-15-155-24.us-east-2.compute.amazonaws.com "sudo docker pull nayantara52/nayan-docker-images:v1"
    - name: Run Image on EC2
      run: |
        ssh -o StrictHostKeyChecking=no -i ./ec2-key-pair.pem ec2-user@ec2-3-15-155-24.us-east-2.compute.amazonaws.com "sudo docker run -d -p 8024:80 nayantara52/nayan-docker-images:v1"

    

    

  


