name: CI-CD pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    

    steps:
    - name: checkout
      uses: actions/checkout@v2

    - name: Docker Login
      uses: docker/login-action@v1
      with:
        username: nayantara52
        password: Vkssann@2805

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: nayantara52/nayan-docker-images:v1

    - name: AWS Login
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIARWAZI6BRA2BMG36N
        aws-secret-access-key: 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
        aws-region: us-east-2

    - name: pull image to ec2
      run: |
        sudo docker pull nayantara52/nayan-docker-images:v1 

    - name: run image
      run: |
        sudo docker run -d -p 80:80 nayantara52/nayan-docker-images:v1



    

  


